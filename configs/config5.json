{
  "cmd": "docker build -t imrsnn . && nvidia-docker run -v /home/ubuntu/eric/rsnn:/home/developer --name config5 --net host --ipc host --rm -t imrsnn python3 Code/run_exp.py config5",
  "mem_req": 2500,
  "params": {
    "spkfn": "bellec",
    "spkconfig": 0,
    "architecture": "1L",
    "beta": 0.95,
    "control_neuron": "LIF",
    "mem_neuron": "Adaptive",
    "lr": 0.01,
    "1-beta": false,
    "decay_out": true,
    "ported_weights": false,
    "NoBias": false
  }
}