{
  "cmd": "docker build -t imrsnn . && nvidia-docker run -v /home/ubuntu/eric/rsnn:/home/developer --name config2 --net host --ipc host --rm -t imrsnn python3 Code/run_exp.py config2",
  "mem_req": 3000000000,
  "params": {
    "spkfn": "bellec",
    "spkconfig": 0,
    "architecture": "1L",
    "beta": 0.95,
    "control_neuron": "LIF",
    "mem_neuron": "Adaptive",
    "lr": 0.001,
    "1-beta": false,
    "decay_out": true
  }
}